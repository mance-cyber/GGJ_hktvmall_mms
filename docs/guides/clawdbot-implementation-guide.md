# GoGoJap + Clawdbot 整合實施指南

> 版本：1.0
> 更新日期：2026-01-26
> 作者：GoGoJap 開發團隊

---

## 📋 目錄

1. [系統架構](#系統架構)
2. [前置需求](#前置需求)
3. [部署步驟](#部署步驟)
4. [測試驗證](#測試驗證)
5. [使用說明](#使用說明)
6. [故障排除](#故障排除)
7. [性能優化](#性能優化)

---

## 🏗️ 系統架構

### 整體架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                    GoGoJap Frontend                          │
│                    (Next.js 14)                              │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  測試面板: /scrape/clawdbot-test                   │     │
│  │  - 商品抓取測試                                     │     │
│  │  - SEO 排名測試                                     │     │
│  │  - 批量抓取測試                                     │     │
│  └──────────────────┬─────────────────────────────────┘     │
│                     │                                        │
└─────────────────────┼────────────────────────────────────────┘
                      │
                      │ HTTP POST /api/v1/scrape/clawdbot
                      │
┌─────────────────────▼────────────────────────────────────────┐
│                  GoGoJap Backend API                         │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  ClawdbotConnector (lib/connectors/)                │    │
│  │  - WebSocket 連接管理                                │    │
│  │  - 任務調度                                          │    │
│  │  - 速率限制                                          │    │
│  │  - 錯誤處理                                          │    │
│  └──────────────────┬──────────────────────────────────┘    │
│                     │                                        │
└─────────────────────┼────────────────────────────────────────┘
                      │
                      │ WebSocket ws://127.0.0.1:18789
                      │
┌─────────────────────▼────────────────────────────────────────┐
│                  Clawdbot Server                             │
│                  (本地運行)                                   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  WebSocket Gateway                                   │    │
│  │  - 接收任務                                          │    │
│  │  - 分配到瀏覽器池                                     │    │
│  │  - 返回結果                                          │    │
│  └──────────────────┬──────────────────────────────────┘    │
│                     │                                        │
│  ┌──────────────────▼──────────────────────────────────┐    │
│  │  Browser Pool (Chrome Headless)                     │    │
│  │  - 實例 1: 商品抓取                                  │    │
│  │  - 實例 2: SEO 追蹤                                  │    │
│  │  - 實例 3: 競品監控                                  │    │
│  └──────────────────┬──────────────────────────────────┘    │
│                     │                                        │
│  ┌──────────────────▼──────────────────────────────────┐    │
│  │  GoGoJap Skills (skills/gogojap/)                   │    │
│  │  - hktv-product-scraper.md                          │    │
│  │  - seo-rank-tracker.md                              │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       │ HTTPS
                       │
┌──────────────────────▼───────────────────────────────────────┐
│                  目標網站                                     │
│  - HKTVmall (www.hktvmall.com)                              │
│  - Google 香港 (www.google.com.hk)                          │
└──────────────────────────────────────────────────────────────┘
```

### 數據流程

```
用戶操作 → Frontend → API → ClawdbotConnector → Clawdbot → Browser → 目標網站
                                                                    ↓
用戶查看 ← Frontend ← API ← ClawdbotConnector ← Clawdbot ← Browser ← 抓取數據
```

---

## 🔧 前置需求

### 系統需求

| 項目 | 最低要求 | 推薦配置 |
|-----|---------|---------|
| 作業系統 | Windows 10 / macOS 10.15+ / Linux | Windows 11 / macOS 13+ |
| Node.js | >= 18.17.0 | >= 22.0.0 |
| RAM | 4GB | 8GB+ |
| 硬碟空間 | 2GB | 5GB+ |
| 網絡 | 穩定網絡連接 | 10Mbps+ |

### 軟件需求

- [x] Node.js >= 22 ([下載](https://nodejs.org/))
- [x] pnpm ([安裝](https://pnpm.io/installation))
- [x] Git ([下載](https://git-scm.com/))
- [x] Chrome/Chromium 瀏覽器

### API Keys

- [x] Anthropic API Key ([獲取](https://console.anthropic.com/))
  - 用於 clawdbot 的 AI 決策引擎
  - 推薦使用 Claude Opus 4.5 或 Sonnet 4.5

---

## 🚀 部署步驟

### 步驟 1: 部署 Clawdbot

#### 1.1 運行部署腳本

在 GoGoJap 項目根目錄執行：

```bash
# Windows (Git Bash)
bash scripts/setup-clawdbot.sh

# Linux/macOS
chmod +x scripts/setup-clawdbot.sh
./scripts/setup-clawdbot.sh
```

腳本會自動：
- ✅ 檢查系統環境 (Node.js, pnpm)
- ✅ 克隆 clawdbot 倉庫
- ✅ 安裝依賴
- ✅ 創建配置文件 (.env)
- ✅ 創建 GoGoJap 專用 Skills

#### 1.2 配置 API Key

編輯 `clawdbot/.env` 文件：

```env
# 必填: 你的 Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# 可選: OpenAI API Key (如果想用 ChatGPT)
# OPENAI_API_KEY=sk-xxxxx

# WebSocket Gateway 端口
PORT=18789

# 日誌級別
LOG_LEVEL=info

# 瀏覽器設置
BROWSER_HEADLESS=true      # 無頭模式 (生產環境設為 true)
BROWSER_POOL_SIZE=3        # 瀏覽器實例數量

# GoGoJap 專用配置
GOGOJAP_MODE=scraping
GOGOJAP_RATE_LIMIT=30      # 每分鐘最多 30 次請求
```

#### 1.3 啟動 Clawdbot

```bash
# 方式 1: 使用啟動腳本
./start-clawdbot.sh

# 方式 2: 手動啟動
cd clawdbot
pnpm start
```

成功啟動後，你會看到：

```
✅ Clawdbot Gateway 已啟動
🌐 WebSocket 監聽: ws://127.0.0.1:18789
🔧 瀏覽器池大小: 3
📊 Skills 已加載: hktv-product-scraper, seo-rank-tracker
```

---

### 步驟 2: 安裝 GoGoJap 依賴

確保 `ws` (WebSocket) 包已安裝：

```bash
npm install ws
npm install --save-dev @types/ws
```

---

### 步驟 3: 啟動 GoGoJap

```bash
npm run dev
```

GoGoJap 會在 http://localhost:3000 啟動。

---

## ✅ 測試驗證

### 測試 1: 健康檢查

1. 瀏覽器打開: http://localhost:3000/scrape/clawdbot-test
2. 查看 "Clawdbot 服務狀態" 卡片
3. 確認狀態顯示為 "已連接" (綠色)

如果顯示 "未連接":
- 檢查 clawdbot 是否正在運行
- 檢查 WebSocket 端口 18789 是否被占用

### 測試 2: 抓取單個商品

1. 切換到 "抓取商品" Tab
2. 輸入測試 URL (示例):
   ```
   https://www.hktvmall.com/hktv/zh/main/gogojap-123456
   ```
3. 點擊 "開始抓取"
4. 等待 10-20 秒
5. 查看抓取結果 (JSON 格式)

**預期結果**:
```json
{
  "success": true,
  "data": {
    "name": "日本零食套裝",
    "price": 99.00,
    "originalPrice": 129.00,
    "discountPercent": 23,
    "stockStatus": "in_stock",
    "brand": "日本品牌",
    ...
  },
  "metadata": {
    "taskId": "task_1234567890_abc123",
    "durationMs": 15234,
    "scrapedAt": "2026-01-26T10:30:00Z"
  }
}
```

### 測試 3: 搜尋排名追蹤

1. 切換到 "搜尋排名" Tab
2. 輸入:
   - 關鍵詞: `日本零食`
   - 目標 URL: `https://www.hktvmall.com/hktv/zh/main/gogojap-123456`
3. 點擊 "查詢排名"
4. 查看排名結果

**預期結果**:
```json
{
  "success": true,
  "data": {
    "keyword": "日本零食",
    "targetUrl": "https://...",
    "currentRank": 5,
    "totalResults": 234,
    "topCompetitors": [...]
  }
}
```

### 測試 4: 批量抓取

1. 切換到 "批量抓取" Tab
2. 輸入多個 URL (每行一個):
   ```
   https://www.hktvmall.com/product1
   https://www.hktvmall.com/product2
   https://www.hktvmall.com/product3
   ```
3. 點擊 "開始批量抓取"
4. 查看批量結果

**預期結果**:
```json
{
  "success": true,
  "data": {
    "total": 3,
    "successCount": 3,
    "failCount": 0,
    "results": [...]
  }
}
```

---

## 📖 使用說明

### 在代碼中使用 Clawdbot

#### 方式 1: 使用 Hook (推薦)

```typescript
'use client';

import { useClawdbot } from '@/lib/hooks/use-clawdbot';

export default function MyComponent() {
  const { scrapeProduct, isScrapingProduct } = useClawdbot();

  const handleScrape = async () => {
    const result = await scrapeProduct('https://www.hktvmall.com/...');
    console.log('抓取結果:', result);
  };

  return (
    <button onClick={handleScrape} disabled={isScrapingProduct}>
      {isScrapingProduct ? '抓取中...' : '開始抓取'}
    </button>
  );
}
```

#### 方式 2: 直接調用 API

```typescript
const response = await fetch('/api/v1/scrape/clawdbot', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'scrape_product',
    params: {
      url: 'https://www.hktvmall.com/...'
    }
  })
});

const result = await response.json();
```

#### 方式 3: 使用 Connector (後端)

```typescript
import { getClawdbotConnector } from '@/lib/connectors/clawdbot-connector';

const connector = getClawdbotConnector();
const result = await connector.scrapeHKTVProduct('https://www.hktvmall.com/...');
```

---

## 🔍 故障排除

### 問題 1: Clawdbot 連接失敗

**症狀**: Frontend 顯示 "未連接"

**解決方案**:
1. 確認 clawdbot 正在運行:
   ```bash
   ps aux | grep clawdbot  # Linux/macOS
   tasklist | findstr node  # Windows
   ```

2. 檢查端口是否被占用:
   ```bash
   netstat -an | grep 18789  # Linux/macOS
   netstat -an | findstr 18789  # Windows
   ```

3. 檢查防火牆設置 (允許 localhost:18789)

4. 查看 clawdbot 日誌:
   ```bash
   cd clawdbot
   pnpm logs
   ```

### 問題 2: 抓取超時

**症狀**: 任務一直在 "抓取中..."，最後超時

**原因**:
- 目標網站加載緩慢
- 網絡不穩定
- 瀏覽器資源不足

**解決方案**:
1. 增加超時時間:
   ```typescript
   const connector = getClawdbotConnector({
     timeout: 120000  // 120 秒
   });
   ```

2. 增加瀏覽器實例數:
   ```env
   # clawdbot/.env
   BROWSER_POOL_SIZE=5
   ```

3. 檢查網絡連接

### 問題 3: 抓取數據不完整

**症狀**: 某些字段為 `null`

**原因**:
- 網頁結構變化
- CSS 選擇器不匹配
- 內容動態加載

**解決方案**:
1. 更新 CSS 選擇器:
   ```typescript
   // 在 ClawdbotConnector 中調整 extractors
   extractors: [
     { field: 'name', selector: '.new-product-title' }  // 更新選擇器
   ]
   ```

2. 添加等待時間:
   ```typescript
   actions: [
     { type: 'wait', delay: 5000 }  // 等待 5 秒
   ]
   ```

3. 啟用截圖調試:
   ```typescript
   config: {
     screenshot: true  // 保存截圖以檢查頁面狀態
   }
   ```

### 問題 4: 速率限制錯誤

**症狀**: "達到速率限制" 錯誤

**解決方案**:
調整速率限制配置:
```typescript
const connector = getClawdbotConnector({
  rateLimitPerMinute: 20  // 降低到每分鐘 20 次
});
```

---

## ⚡ 性能優化

### 1. 調整瀏覽器池大小

根據系統資源調整:

```env
# 低配置 (4GB RAM)
BROWSER_POOL_SIZE=2

# 中等配置 (8GB RAM)
BROWSER_POOL_SIZE=3

# 高配置 (16GB+ RAM)
BROWSER_POOL_SIZE=5
```

### 2. 啟用請求緩存

對於不頻繁變化的數據，啟用緩存:

```typescript
// 緩存商品數據 1 小時
const cacheKey = `product_${productUrl}`;
const cached = await redis.get(cacheKey);

if (cached) {
  return JSON.parse(cached);
}

const result = await connector.scrapeHKTVProduct(productUrl);
await redis.setex(cacheKey, 3600, JSON.stringify(result));
```

### 3. 批量處理策略

對於大量 URL，分批處理:

```typescript
async function scrapeLargeList(urls: string[]) {
  const BATCH_SIZE = 10;
  const results = [];

  for (let i = 0; i < urls.length; i += BATCH_SIZE) {
    const batch = urls.slice(i, i + BATCH_SIZE);
    const batchResults = await connector.scrapeBatch(batch);
    results.push(...batchResults);

    // 批次間休息 30 秒
    await new Promise(resolve => setTimeout(resolve, 30000));
  }

  return results;
}
```

### 4. 監控與日誌

啟用詳細日誌以追蹤性能:

```env
# clawdbot/.env
LOG_LEVEL=debug
```

查看日誌:
```bash
cd clawdbot
tail -f logs/clawdbot.log
```

---

## 📊 成本與效益分析

### 與 Firecrawl 對比

| 維度 | Firecrawl (雲端) | Clawdbot (本地) |
|-----|-----------------|----------------|
| **設置成本** | 0 (即用即付) | 高 (需部署配置) |
| **運行成本** | 按請求付費 (~$0.01/次) | 本地資源 (幾乎免費) |
| **抓取能力** | 受 API 限制 | 完全自定義 |
| **複雜場景** | 有限 | 完全支持 |
| **維護成本** | 低 (SaaS) | 中等 (自維護) |
| **擴展性** | 無限 | 受本地資源限制 |

### ROI 預估

假設每日抓取 1000 個商品頁面:

**Firecrawl 方案**:
- 成本: 1000 × $0.01 = $10/天 = $300/月

**Clawdbot 方案**:
- 成本: Anthropic API (~$50/月) + 本地服務器 (~$0)
- **節省**: ~$250/月 ≈ 83%

---

## 🎯 下一步計劃

### 短期 (1-2 週)
- [ ] 完成 Clawdbot 本地部署
- [ ] 測試商品抓取功能
- [ ] 測試 SEO 排名追蹤
- [ ] 收集性能數據

### 中期 (1 個月)
- [ ] 整合到生產環境
- [ ] 替換 Firecrawl (或並行運行)
- [ ] 實現自動化定時任務
- [ ] 添加抓取失敗重試機制

### 長期 (3 個月)
- [ ] 容器化部署 (Docker)
- [ ] 實現分佈式抓取 (多節點)
- [ ] 添加更多平台支持 (淘寶、亞馬遜等)
- [ ] 構建抓取數據分析儀表板

---

## 📞 支持與反饋

遇到問題？

1. 查看本文檔的 [故障排除](#故障排除) 章節
2. 查看 clawdbot 官方文檔: https://docs.clawd.bot
3. 提交 Issue: https://github.com/clawdbot/clawdbot/issues
4. GoGoJap 內部支持: 聯繫開發團隊

---

**版本歷史**

- v1.0 (2026-01-26): 初始版本，完成 Clawdbot 整合
