# HKTVmall 類別數據庫系統設計

> 大規模商品數據抓取 + AI 市場分析

---

## 🎯 功能概述

### 核心能力

**1. 類別批量抓取**
- 一鍵抓取整個類別的所有商品（如：和牛類、急凍肉類）
- 自動發現該類別下的所有商品 URL
- 批量提取商品資訊並儲存

**2. 類別數據庫**
- 按商品類別組織數據
- 儲存完整的商品資訊、價格歷史、庫存狀態
- 支援多維度查詢和過濾

**3. AI 市場分析**
- 分析類別內的價格分佈
- 識別市場趨勢和機會
- 提供定價建議
- 生成競爭分析報告

---

## 🏗️ 系統架構

### 整體流程

```
┌────────────────────────────────────────────────────────┐
│                   1. 類別發現與抓取                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│  使用者輸入類別 URL                                     │
│         ↓                                              │
│  系統使用 Firecrawl Map 發現所有商品                    │
│         ↓                                              │
│  批量抓取每個商品的詳細資訊                             │
│         ↓                                              │
│  儲存到類別數據庫                                       │
│                                                        │
└────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│                   2. 數據清洗與標準化                    │
├────────────────────────────────────────────────────────┤
│                                                        │
│  統一單位（克、公斤、件）                               │
│         ↓                                              │
│  標準化品牌名稱                                         │
│         ↓                                              │
│  提取關鍵屬性（產地、等級、重量）                       │
│         ↓                                              │
│  計算單價（每 100g 價格）                               │
│                                                        │
└────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│                   3. AI 智能分析                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│  價格分佈分析 → 找出價格帶                              │
│         ↓                                              │
│  競爭格局分析 → 識別主要玩家                            │
│         ↓                                              │
│  趨勢預測 → 預測價格走向                                │
│         ↓                                              │
│  機會識別 → 發現市場空白                                │
│         ↓                                              │
│  生成報告 → 可執行的建議                                │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 📊 數據庫設計

### 新增的數據表

#### 1. `category_databases` - 類別數據庫表

```sql
CREATE TABLE category_databases (
    id UUID PRIMARY KEY,
    name VARCHAR(255) NOT NULL,              -- 如：「和牛類」
    description TEXT,                         -- 類別描述
    hktv_category_url TEXT,                  -- HKTVmall 類別頁面 URL
    total_products INTEGER DEFAULT 0,         -- 類別內商品總數
    last_scraped_at TIMESTAMP,               -- 最後抓取時間
    scrape_frequency VARCHAR(50),            -- 抓取頻率
    is_active BOOLEAN DEFAULT TRUE,          -- 是否啟用
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

#### 2. `category_products` - 類別商品表

```sql
CREATE TABLE category_products (
    id UUID PRIMARY KEY,
    category_id UUID REFERENCES category_databases(id),

    -- 基本資訊
    name VARCHAR(500) NOT NULL,
    url TEXT NOT NULL,
    sku VARCHAR(255),
    brand VARCHAR(255),

    -- 價格資訊
    price DECIMAL(10, 2),
    original_price DECIMAL(10, 2),
    discount_percent DECIMAL(5, 2),

    -- 商品屬性（JSON 格式）
    attributes JSONB,                        -- {
                                             --   "origin": "日本",
                                             --   "grade": "A5",
                                             --   "weight": "500g",
                                             --   "cut": "西冷"
                                             -- }

    -- 標準化單價（用於對比）
    unit_price DECIMAL(10, 2),              -- 每 100g 價格
    unit_type VARCHAR(50),                   -- 單位類型

    -- 狀態
    stock_status VARCHAR(50),
    is_available BOOLEAN DEFAULT TRUE,

    -- 評價
    rating DECIMAL(3, 2),
    review_count INTEGER,

    -- 圖片
    image_url TEXT,

    -- 時間戳
    first_seen_at TIMESTAMP DEFAULT NOW(),
    last_updated_at TIMESTAMP DEFAULT NOW(),

    UNIQUE(category_id, url)
);
```

#### 3. `category_price_snapshots` - 類別商品價格快照

```sql
CREATE TABLE category_price_snapshots (
    id UUID PRIMARY KEY,
    category_product_id UUID REFERENCES category_products(id),

    price DECIMAL(10, 2),
    original_price DECIMAL(10, 2),
    discount_percent DECIMAL(5, 2),
    unit_price DECIMAL(10, 2),              -- 標準化單價

    stock_status VARCHAR(50),
    is_available BOOLEAN,

    scraped_at TIMESTAMP DEFAULT NOW()
);
```

#### 4. `category_analysis_reports` - AI 分析報告表

```sql
CREATE TABLE category_analysis_reports (
    id UUID PRIMARY KEY,
    category_id UUID REFERENCES category_databases(id),

    report_type VARCHAR(50),                 -- price_analysis, competition, trend
    report_title VARCHAR(255),

    -- AI 生成的分析內容
    summary TEXT,                            -- 摘要
    key_findings JSONB,                      -- 關鍵發現
    recommendations JSONB,                   -- 建議

    -- 數據快照
    data_snapshot JSONB,                     -- 分析時的數據快照

    -- 元數據
    generated_by VARCHAR(50),                -- AI 模型名稱
    generated_at TIMESTAMP DEFAULT NOW(),

    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🔧 後端實現

### 1. 類別抓取器 (Category Scraper)

新增 `backend/app/connectors/category_scraper.py`：

```python
# =============================================
# 類別批量抓取器
# =============================================

from typing import List, Dict, Any
from app.connectors.firecrawl import get_firecrawl_connector
from app.models.category import CategoryDatabase, CategoryProduct
from decimal import Decimal
import re


class CategoryScraper:
    """HKTVmall 類別批量抓取器"""

    def __init__(self):
        self.connector = get_firecrawl_connector()

    async def discover_category_products(
        self,
        category_url: str,
        max_products: int = 500
    ) -> List[str]:
        """
        發現類別頁面下的所有商品 URL

        Args:
            category_url: HKTVmall 類別頁面 URL
            max_products: 最大商品數量

        Returns:
            商品 URL 列表
        """
        # 使用 Firecrawl Map 發現所有 URL
        map_result = self.connector.map_urls(
            url=category_url,
            limit=max_products
        )

        # 過濾出商品頁面 URL
        product_urls = []
        for url in map_result.urls:
            if self._is_product_url(url):
                product_urls.append(url)

        return product_urls

    def _is_product_url(self, url: str) -> bool:
        """判斷是否為商品頁面 URL"""
        # HKTVmall 商品頁面特徵
        patterns = [
            r'/p/[A-Z0-9_-]+',           # /p/H1234567
            r'/products?/',
            r'/item/',
        ]

        for pattern in patterns:
            if re.search(pattern, url, re.IGNORECASE):
                return True

        return False

    async def scrape_category(
        self,
        category_id: str,
        category_url: str,
        db
    ) -> Dict[str, Any]:
        """
        批量抓取類別內所有商品

        Args:
            category_id: 類別 ID
            category_url: 類別頁面 URL
            db: 資料庫 session

        Returns:
            抓取統計
        """
        stats = {
            'total_discovered': 0,
            'total_scraped': 0,
            'total_failed': 0,
            'new_products': 0,
            'updated_products': 0
        }

        # 1. 發現所有商品 URL
        product_urls = await self.discover_category_products(category_url)
        stats['total_discovered'] = len(product_urls)

        # 2. 批量抓取商品資訊
        for url in product_urls:
            try:
                # 抓取商品
                info = self.connector.extract_product_info(url)

                # 儲存或更新商品
                product = await self._save_or_update_product(
                    category_id=category_id,
                    url=url,
                    info=info,
                    db=db
                )

                if product.first_seen_at == product.last_updated_at:
                    stats['new_products'] += 1
                else:
                    stats['updated_products'] += 1

                stats['total_scraped'] += 1

            except Exception as e:
                stats['total_failed'] += 1
                print(f"抓取失敗: {url} - {e}")

        return stats

    async def _save_or_update_product(
        self,
        category_id: str,
        url: str,
        info,
        db
    ):
        """儲存或更新商品資訊"""
        from sqlalchemy import select
        from datetime import datetime

        # 查找是否已存在
        result = await db.execute(
            select(CategoryProduct).where(
                CategoryProduct.category_id == category_id,
                CategoryProduct.url == url
            )
        )
        product = result.scalar_one_or_none()

        # 提取並標準化屬性
        attributes = self._extract_attributes(info)
        unit_price = self._calculate_unit_price(info, attributes)

        if product:
            # 更新現有商品
            product.name = info.name
            product.price = info.price
            product.original_price = info.original_price
            product.discount_percent = info.discount_percent
            product.stock_status = info.stock_status
            product.rating = info.rating
            product.review_count = info.review_count
            product.image_url = info.image_url
            product.attributes = attributes
            product.unit_price = unit_price
            product.last_updated_at = datetime.utcnow()

            # 創建價格快照
            from app.models.category import CategoryPriceSnapshot
            snapshot = CategoryPriceSnapshot(
                category_product_id=product.id,
                price=info.price,
                original_price=info.original_price,
                unit_price=unit_price,
                stock_status=info.stock_status
            )
            db.add(snapshot)
        else:
            # 新增商品
            product = CategoryProduct(
                category_id=category_id,
                name=info.name,
                url=url,
                sku=info.sku,
                brand=info.brand,
                price=info.price,
                original_price=info.original_price,
                discount_percent=info.discount_percent,
                stock_status=info.stock_status,
                rating=info.rating,
                review_count=info.review_count,
                image_url=info.image_url,
                attributes=attributes,
                unit_price=unit_price
            )
            db.add(product)

        await db.flush()
        return product

    def _extract_attributes(self, info) -> Dict[str, Any]:
        """從商品名稱和描述中提取屬性"""
        attributes = {}

        name = info.name.lower()

        # 提取產地
        origins = ['日本', '澳洲', '美國', '紐西蘭', '加拿大']
        for origin in origins:
            if origin in name:
                attributes['origin'] = origin
                break

        # 提取等級（和牛專用）
        grades = ['a5', 'a4', 'a3']
        for grade in grades:
            if grade in name:
                attributes['grade'] = grade.upper()
                break

        # 提取重量
        weight_match = re.search(r'(\d+(?:\.\d+)?)\s*(g|kg|克|公斤)', name)
        if weight_match:
            value = float(weight_match.group(1))
            unit = weight_match.group(2)

            # 統一轉換為克
            if unit in ['kg', '公斤']:
                value *= 1000

            attributes['weight'] = value
            attributes['weight_unit'] = 'g'

        # 提取部位（肉類專用）
        cuts = ['西冷', '肉眼', '牛柳', '牛腱', '牛肋', '牛腩']
        for cut in cuts:
            if cut in name:
                attributes['cut'] = cut
                break

        return attributes

    def _calculate_unit_price(self, info, attributes: Dict) -> Decimal:
        """計算標準化單價（每 100g 價格）"""
        if not info.price:
            return None

        weight = attributes.get('weight')
        if not weight or weight <= 0:
            return None

        # 計算每 100g 價格
        unit_price = (info.price / Decimal(weight)) * Decimal(100)
        return round(unit_price, 2)


# 單例
_scraper = None

def get_category_scraper() -> CategoryScraper:
    """獲取類別抓取器單例"""
    global _scraper
    if _scraper is None:
        _scraper = CategoryScraper()
    return _scraper
```

---

### 2. AI 市場分析器

新增 `backend/app/analyzers/market_analyzer.py`：

```python
# =============================================
# AI 市場分析器
# =============================================

from typing import List, Dict, Any
from decimal import Decimal
from app.connectors.claude import get_claude_client
import json


class MarketAnalyzer:
    """AI 驅動的市場分析器"""

    def __init__(self):
        self.claude = get_claude_client()

    async def analyze_category(
        self,
        category_id: str,
        db
    ) -> Dict[str, Any]:
        """
        分析類別市場狀況

        Returns:
            {
                'summary': str,
                'price_distribution': {...},
                'competition_landscape': {...},
                'opportunities': [...],
                'recommendations': [...]
            }
        """
        # 1. 獲取類別數據
        category_data = await self._get_category_data(category_id, db)

        # 2. 準備分析提示詞
        prompt = self._build_analysis_prompt(category_data)

        # 3. 呼叫 Claude AI 分析
        analysis = await self._call_claude_analysis(prompt)

        # 4. 儲存分析報告
        await self._save_analysis_report(category_id, analysis, db)

        return analysis

    async def _get_category_data(self, category_id: str, db) -> Dict[str, Any]:
        """獲取類別的所有數據"""
        from sqlalchemy import select, func
        from app.models.category import CategoryProduct, CategoryDatabase

        # 獲取類別資訊
        result = await db.execute(
            select(CategoryDatabase).where(CategoryDatabase.id == category_id)
        )
        category = result.scalar_one()

        # 獲取所有商品
        result = await db.execute(
            select(CategoryProduct).where(
                CategoryProduct.category_id == category_id,
                CategoryProduct.is_available == True
            )
        )
        products = result.scalars().all()

        # 整理數據
        data = {
            'category_name': category.name,
            'total_products': len(products),
            'products': []
        }

        for p in products:
            data['products'].append({
                'name': p.name,
                'brand': p.brand,
                'price': float(p.price) if p.price else None,
                'unit_price': float(p.unit_price) if p.unit_price else None,
                'discount_percent': float(p.discount_percent) if p.discount_percent else None,
                'rating': float(p.rating) if p.rating else None,
                'review_count': p.review_count,
                'attributes': p.attributes
            })

        return data

    def _build_analysis_prompt(self, data: Dict[str, Any]) -> str:
        """構建給 Claude 的分析提示詞"""

        products_json = json.dumps(data['products'], ensure_ascii=False, indent=2)

        prompt = f"""你是一位資深的電商市場分析師。請分析以下 HKTVmall 「{data['category_name']}」類別的市場數據。

# 商品數據
共 {data['total_products']} 個商品

```json
{products_json}
```

# 分析要求

請提供以下分析（以 JSON 格式回覆）：

1. **價格分佈分析** (price_distribution)
   - 最低價、最高價、平均價、中位數
   - 價格區間分佈（如：低價區 < $100, 中價區 $100-200, 高價區 > $200）
   - 單價分析（每 100g 價格）

2. **競爭格局分析** (competition_landscape)
   - 主要品牌及其市場佔有率
   - 品牌定位（高端、中端、平價）
   - 產地分佈

3. **市場機會** (opportunities)
   - 價格空白區（哪些價格帶沒有商品）
   - 產品空白區（哪些屬性組合缺乏）
   - 低競爭高需求區

4. **定價建議** (pricing_recommendations)
   - 建議的定價策略
   - 不同定位的建議價格點
   - 理由說明

5. **摘要** (summary)
   - 用 2-3 句話總結市場狀況

請以以下 JSON 格式回覆（純 JSON，不要 markdown 代碼塊）：

{{
  "summary": "市場摘要...",
  "price_distribution": {{
    "min_price": 100,
    "max_price": 500,
    "avg_price": 250,
    "median_price": 230,
    "price_ranges": [
      {{"range": "< $100", "count": 10, "percentage": 20}},
      {{"range": "$100-200", "count": 25, "percentage": 50}},
      {{"range": "> $200", "count": 15, "percentage": 30}}
    ]
  }},
  "competition_landscape": {{
    "top_brands": [
      {{"brand": "品牌A", "product_count": 20, "avg_price": 300}},
      {{"brand": "品牌B", "product_count": 15, "avg_price": 200}}
    ],
    "origin_distribution": [
      {{"origin": "日本", "count": 30}},
      {{"origin": "澳洲", "count": 20}}
    ]
  }},
  "opportunities": [
    {{
      "type": "price_gap",
      "description": "$150-180 價格帶商品較少，有空間進入",
      "potential": "high"
    }}
  ],
  "pricing_recommendations": [
    {{
      "positioning": "中高端",
      "suggested_price": 280,
      "reason": "市場平均價 $250，略高定位可突顯品質"
    }}
  ]
}}
"""
        return prompt

    async def _call_claude_analysis(self, prompt: str) -> Dict[str, Any]:
        """呼叫 Claude AI 進行分析"""

        response = self.claude.messages.create(
            model="claude-sonnet-4-20250514",
            max_tokens=4096,
            messages=[{
                "role": "user",
                "content": prompt
            }]
        )

        # 解析 JSON 回應
        content = response.content[0].text

        # 移除可能的 markdown 代碼塊標記
        content = content.strip()
        if content.startswith('```json'):
            content = content[7:]
        if content.startswith('```'):
            content = content[3:]
        if content.endswith('```'):
            content = content[:-3]
        content = content.strip()

        analysis = json.loads(content)

        return analysis

    async def _save_analysis_report(
        self,
        category_id: str,
        analysis: Dict[str, Any],
        db
    ):
        """儲存分析報告到資料庫"""
        from app.models.category import CategoryAnalysisReport

        report = CategoryAnalysisReport(
            category_id=category_id,
            report_type='comprehensive',
            report_title=f'{analysis.get("summary", "市場分析報告")}',
            summary=analysis.get('summary'),
            key_findings=analysis,
            generated_by='claude-sonnet-4'
        )

        db.add(report)
        await db.flush()


# 單例
_analyzer = None

def get_market_analyzer() -> MarketAnalyzer:
    """獲取市場分析器單例"""
    global _analyzer
    if _analyzer is None:
        _analyzer = MarketAnalyzer()
    return _analyzer
```

---

### 3. API 端點

新增 `backend/app/api/v1/categories.py`：

```python
# =============================================
# 類別數據庫 API
# =============================================

from fastapi import APIRouter, Depends, HTTPException, BackgroundTasks
from sqlalchemy.ext.asyncio import AsyncSession
from typing import Optional

from app.models.database import get_db
from app.connectors.category_scraper import get_category_scraper
from app.analyzers.market_analyzer import get_market_analyzer

router = APIRouter()


@router.post("/categories")
async def create_category_database(
    name: str,
    hktv_category_url: str,
    description: Optional[str] = None,
    db: AsyncSession = Depends(get_db)
):
    """創建新的類別數據庫"""
    from app.models.category import CategoryDatabase

    category = CategoryDatabase(
        name=name,
        description=description,
        hktv_category_url=hktv_category_url
    )

    db.add(category)
    await db.flush()
    await db.refresh(category)

    return {
        "id": str(category.id),
        "name": category.name,
        "message": "類別數據庫已創建，可以開始抓取"
    }


@router.post("/categories/{category_id}/scrape")
async def scrape_category(
    category_id: str,
    background_tasks: BackgroundTasks,
    db: AsyncSession = Depends(get_db)
):
    """觸發類別批量抓取"""
    from app.models.category import CategoryDatabase
    from sqlalchemy import select

    # 獲取類別資訊
    result = await db.execute(
        select(CategoryDatabase).where(CategoryDatabase.id == category_id)
    )
    category = result.scalar_one_or_none()

    if not category:
        raise HTTPException(status_code=404, detail="類別不存在")

    # 在背景執行抓取任務
    scraper = get_category_scraper()
    background_tasks.add_task(
        scraper.scrape_category,
        category_id=category_id,
        category_url=category.hktv_category_url,
        db=db
    )

    return {
        "message": f"已開始抓取「{category.name}」類別",
        "category_id": category_id
    }


@router.get("/categories/{category_id}/products")
async def get_category_products(
    category_id: str,
    page: int = 1,
    limit: int = 50,
    sort_by: str = "price",  # price, unit_price, rating
    db: AsyncSession = Depends(get_db)
):
    """獲取類別內的所有商品"""
    from app.models.category import CategoryProduct
    from sqlalchemy import select, desc, asc

    query = select(CategoryProduct).where(
        CategoryProduct.category_id == category_id,
        CategoryProduct.is_available == True
    )

    # 排序
    if sort_by == "price":
        query = query.order_by(asc(CategoryProduct.price))
    elif sort_by == "unit_price":
        query = query.order_by(asc(CategoryProduct.unit_price))
    elif sort_by == "rating":
        query = query.order_by(desc(CategoryProduct.rating))

    # 分頁
    query = query.offset((page - 1) * limit).limit(limit)

    result = await db.execute(query)
    products = result.scalars().all()

    return {
        "products": [
            {
                "id": str(p.id),
                "name": p.name,
                "brand": p.brand,
                "price": float(p.price) if p.price else None,
                "unit_price": float(p.unit_price) if p.unit_price else None,
                "rating": float(p.rating) if p.rating else None,
                "attributes": p.attributes,
                "url": p.url
            }
            for p in products
        ],
        "page": page,
        "limit": limit
    }


@router.post("/categories/{category_id}/analyze")
async def analyze_category_market(
    category_id: str,
    db: AsyncSession = Depends(get_db)
):
    """AI 分析類別市場"""
    analyzer = get_market_analyzer()

    analysis = await analyzer.analyze_category(category_id, db)

    return {
        "category_id": category_id,
        "analysis": analysis
    }


@router.get("/categories/{category_id}/reports")
async def get_category_reports(
    category_id: str,
    db: AsyncSession = Depends(get_db)
):
    """獲取類別的所有分析報告"""
    from app.models.category import CategoryAnalysisReport
    from sqlalchemy import select, desc

    result = await db.execute(
        select(CategoryAnalysisReport)
        .where(CategoryAnalysisReport.category_id == category_id)
        .order_by(desc(CategoryAnalysisReport.generated_at))
    )
    reports = result.scalars().all()

    return {
        "reports": [
            {
                "id": str(r.id),
                "title": r.report_title,
                "summary": r.summary,
                "generated_at": r.generated_at.isoformat()
            }
            for r in reports
        ]
    }
```

---

## 📱 前端界面設計

### 1. 類別管理頁面

```
╔══════════════════════════════════════════════════════════╗
║  類別數據庫管理                        [+ 新增類別]        ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  🥩 和牛類                                                ║
║  ├─ 商品數: 85 個                                        ║
║  ├─ 價格範圍: $188 - $2,580                             ║
║  ├─ 最後更新: 2 小時前                                   ║
║  └─ [查看商品] [重新抓取] [AI 分析] [查看報告]          ║
║                                                          ║
║  ❄️ 急凍肉類                                              ║
║  ├─ 商品數: 142 個                                       ║
║  ├─ 價格範圍: $35 - $680                                ║
║  ├─ 最後更新: 5 小時前                                   ║
║  └─ [查看商品] [重新抓取] [AI 分析] [查看報告]          ║
║                                                          ║
║  🍤 海鮮類                                                ║
║  ├─ 商品數: 0 個 (未抓取)                                ║
║  ├─ 狀態: 等待首次抓取                                   ║
║  └─ [開始抓取] [設定]                                    ║
╚══════════════════════════════════════════════════════════╝
```

### 2. 新增類別界面

```
┌─────────────────────────────────────┐
│ 新增類別數據庫                       │
├─────────────────────────────────────┤
│                                     │
│ 類別名稱 *                           │
│ ┌─────────────────────────────────┐ │
│ │ 和牛類                           │ │
│ └─────────────────────────────────┘ │
│                                     │
│ HKTVmall 類別頁面 URL *             │
│ ┌─────────────────────────────────┐ │
│ │ https://www.hktvmall.com/...    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 類別描述                             │
│ ┌─────────────────────────────────┐ │
│ │ 日本和牛及相關商品               │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 抓取選項                             │
│ ☑ 創建後立即抓取                     │
│ ☑ 抓取完成後自動進行 AI 分析         │
│                                     │
│ 預計抓取商品數: 約 50-100 個         │
│ 預計耗時: 5-10 分鐘                 │
│                                     │
│     [取消]            [創建並抓取]  │
└─────────────────────────────────────┘
```

### 3. 類別商品列表

```
╔══════════════════════════════════════════════════════════╗
║  和牛類 - 商品列表 (85 個)                                ║
╠══════════════════════════════════════════════════════════╣
║  排序: [▼ 單價由低到高]  篩選: [產地▼] [等級▼] [部位▼]  ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ [圖] 日本 A5 和牛西冷 (500g)                        │ ║
║  │      品牌: Premium Wagyu                            │ ║
║  │      價格: $880  原價: $1,200 (折扣 27%)            │ ║
║  │      單價: $176 / 100g                              │ ║
║  │      產地: 日本  等級: A5  部位: 西冷                │ ║
║  │      評分: 4.8 ★ (125 評論)                         │ ║
║  │      [查看詳情]                                      │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ [圖] 澳洲和牛肉眼 (300g)                            │ ║
║  │      品牌: Aussie Beef                              │ ║
║  │      價格: $480  原價: $580 (折扣 17%)              │ ║
║  │      單價: $160 / 100g                              │ ║
║  │      產地: 澳洲  等級: M9  部位: 肉眼                │ ║
║  │      評分: 4.6 ★ (89 評論)                          │ ║
║  │      [查看詳情]                                      │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║                     [第 1 / 9 頁]                        ║
╚══════════════════════════════════════════════════════════╝
```

### 4. AI 分析報告

```
╔══════════════════════════════════════════════════════════╗
║  和牛類 - AI 市場分析報告                                 ║
║  生成時間: 2026-01-06 14:30                              ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  📊 市場摘要                                              ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ 和牛類市場呈現高端化趨勢，日本 A5 和牛佔據          │ ║
║  │ 高價位市場，澳洲和牛提供中價位選擇。價格分佈       │ ║
║  │ 呈現明顯的三個價格帶，$150-180/100g 區間商品       │ ║
║  │ 較少，存在市場機會。                                │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║  💰 價格分佈                                              ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ 單價範圍: $98 - $258 / 100g                         │ ║
║  │ 平均單價: $168 / 100g                               │ ║
║  │ 中位數: $152 / 100g                                 │ ║
║  │                                                     │ ║
║  │ 價格分佈:                                            │ ║
║  │ • 低價區 (< $120)    : 15 個 (18%) ████            │ ║
║  │ • 中價區 ($120-180)  : 45 個 (53%) ████████████    │ ║
║  │ • 高價區 (> $180)    : 25 個 (29%) ██████          │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║  🏆 競爭格局                                              ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ 主要品牌:                                            │ ║
║  │ 1. Premium Wagyu  (20 個) 平均 $195/100g           │ ║
║  │ 2. Aussie Beef    (18 個) 平均 $145/100g           │ ║
║  │ 3. Kobe Premium   (15 個) 平均 $220/100g           │ ║
║  │                                                     │ ║
║  │ 產地分佈:                                            │ ║
║  │ • 日本: 45 個 (53%) - 高端市場                      │ ║
║  │ • 澳洲: 30 個 (35%) - 中端市場                      │ ║
║  │ • 美國: 10 個 (12%) - 平價市場                      │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║  💡 市場機會                                              ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ 1. 價格空白區                                        │ ║
║  │    $150-180/100g 價格帶商品較少，有進入機會        │ ║
║  │    潛力: 🔥🔥🔥 高                                    │ ║
║  │                                                     │ ║
║  │ 2. 產品組合空白                                      │ ║
║  │    美國和牛 + 中價位組合缺乏                        │ ║
║  │    潛力: 🔥🔥 中高                                    │ ║
║  │                                                     │ ║
║  │ 3. 小份量市場                                        │ ║
║  │    200-300g 小包裝商品稀少，適合小家庭              │ ║
║  │    潛力: 🔥🔥 中高                                    │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║  🎯 定價建議                                              ║
║  ┌────────────────────────────────────────────────────┐ ║
║  │ 中高端定位 (推薦)                                    │ ║
║  │ 建議單價: $165-175 / 100g                           │ ║
║  │ 理由: 略高於市場平均，可突顯品質優勢，               │ ║
║  │       同時填補 $150-180 價格空白區                  │ ║
║  │                                                     │ ║
║  │ 中端定位                                             │ ║
║  │ 建議單價: $140-150 / 100g                           │ ║
║  │ 理由: 與主流澳洲和牛競爭，需強化其他優勢            │ ║
║  │                                                     │ ║
║  │ 高端定位                                             │ ║
║  │ 建議單價: $200-220 / 100g                           │ ║
║  │ 理由: 需提供頂級日本 A5 品質，品牌力要求高          │ ║
║  └────────────────────────────────────────────────────┘ ║
║                                                          ║
║              [下載 PDF] [下載 Excel] [分享]              ║
╚══════════════════════════════════════════════════════════╝
```

---

## 🔄 完整使用流程

### 流程 1：首次設定類別數據庫

```
步驟 1: 創建類別
├─ 進入「類別數據庫管理」頁面
├─ 點擊「+ 新增類別」
├─ 輸入類別名稱（如：和牛類）
├─ 貼上 HKTVmall 類別頁面 URL
└─ 點擊「創建並抓取」

步驟 2: 系統自動抓取 (5-10 分鐘)
├─ 發現該類別下所有商品 URL
├─ 批量抓取每個商品的詳細資訊
├─ 提取並標準化商品屬性
├─ 計算單價（每 100g 價格）
└─ 儲存到數據庫

步驟 3: 查看抓取結果
├─ 收到通知「抓取完成」
├─ 查看商品列表
├─ 確認數據正確性
└─ 可以開始分析

步驟 4: AI 市場分析
├─ 點擊「AI 分析」按鈕
├─ 系統自動生成分析報告 (1-2 分鐘)
├─ 查看分析結果
└─ 根據建議調整策略
```

### 流程 2：定期更新數據

```
每週一早上 9:00
├─ 系統自動觸發重新抓取
├─ 更新所有商品的最新價格
├─ 記錄價格變動
└─ 發現新增/下架商品

每月 1 號
├─ 自動生成月度分析報告
├─ 對比上月數據變化
├─ 識別新趨勢
└─ 郵件發送給相關人員
```

### 流程 3：基於分析做決策

```
查看 AI 分析報告
    ↓
發現: $150-180 價格空白區
    ↓
決策: 推出中高端和牛商品
    ↓
定價: $168 / 100g (500g 裝 = $840)
    ↓
上架: 新商品
    ↓
監控: 銷售表現
    ↓
調整: 根據市場反應優化
```

---

## 📈 數據應用場景

### 場景 1：新品定價

**問題**: 準備上架新的日本 A4 和牛，不知道如何定價

**使用方式**:
1. 查看「和牛類」數據庫
2. 篩選「日本」+「A4 等級」商品
3. 查看價格分佈
4. 點擊「AI 分析」→ 查看定價建議
5. 根據建議設定價格

**結果**:
- AI 建議: $185-195 / 100g
- 你的定價: $190 / 100g (500g = $950)
- 比市場平均略高，但在合理範圍內

---

### 場景 2：促銷策略

**問題**: 急凍肉類庫存積壓，需要清倉

**使用方式**:
1. 查看「急凍肉類」數據庫
2. 查看價格分佈和折扣情況
3. AI 分析「目前市場折扣範圍 15-30%」
4. 決定折扣策略

**結果**:
- 市場平均折扣: 20%
- 你的促銷: 25% 折扣
- 價格競爭力提升，加速清倉

---

### 場景 3：發現市場機會

**問題**: 想找新的產品方向

**使用方式**:
1. 查看多個類別的 AI 分析報告
2. 重點關注「市場機會」部分
3. 識別價格空白區、產品空白區

**發現**:
- 和牛類: $150-180 價格帶缺乏
- 海鮮類: 小份量包裝缺乏
- 保健品: 日本品牌 + 平價組合稀少

**行動**:
- 引進填補空白的商品
- 搶佔先機

---

### 場景 4：競爭對手監控

**問題**: 想知道整體市場價格趨勢

**使用方式**:
1. 設定自動每週抓取
2. 對比不同週的數據
3. 查看價格變動趨勢

**洞察**:
- 本月和牛類平均價下降 5%
- 可能是進口成本降低
- 考慮跟進降價或增加行銷

---

## 🎯 成功指標

### 數據完整性

```
目標: 每個類別 > 80% 商品有完整資訊

檢查項目:
☑ 商品名稱
☑ 價格
☑ 品牌
☑ 產地
☑ 重量
☑ 單價

完整度 = 有完整資訊的商品數 / 總商品數
```

### 分析有效性

```
目標: AI 分析建議被採納率 > 60%

追蹤:
- 根據 AI 建議調整的定價數
- 根據 AI 建議引進的新品數
- 實際效果 vs AI 預測
```

### 更新及時性

```
目標: 數據滯後時間 < 24 小時

監控:
- 每個類別的最後更新時間
- 自動抓取任務成功率
- 數據更新頻率
```

---

## 💡 進階功能

### 1. 跨類別對比

```
對比「和牛類」vs「急凍肉類」:
- 價格帶差異
- 折扣力度差異
- 消費者偏好差異
```

### 2. 時間序列分析

```
查看過去 6 個月的價格變化:
- 季節性趨勢
- 促銷週期
- 價格彈性
```

### 3. 競品組合分析

```
發現高轉化的商品組合:
- 常被一起購買的商品
- 互補性商品
- 建議的促銷搭配
```

### 4. 自動化決策

```
基於 AI 分析自動執行:
- 價格過高 → 自動降價 5%
- 發現機會 → 自動生成採購單
- 庫存積壓 → 自動啟動促銷
```

---

## 🔐 注意事項

### 1. 數據合規

- ✅ 只抓取公開商品資訊
- ✅ 遵守 robots.txt
- ✅ 控制抓取頻率
- ❌ 不抓取用戶隱私資訊

### 2. API 配額

- 每個類別首次抓取可能需要 100-500 credits
- 建議分批次抓取
- 優先抓取核心類別

### 3. 數據準確性

- AI 分析基於抓取數據
- 數據品質影響分析品質
- 定期驗證抓取準確性

---

## 📚 總結

這個「類別數據庫系統」讓你能夠：

✅ **一鍵抓取** 整個類別的所有商品
✅ **標準化數據** 統一單位、提取屬性、計算單價
✅ **AI 深度分析** 價格分佈、競爭格局、市場機會
✅ **數據驅動決策** 基於客觀數據而非主觀判斷
✅ **持續監控** 自動更新數據、追蹤市場變化

**從被動應對 → 主動洞察市場**

---

**下一步**: 實現這個系統，開始你的第一個類別數據庫！
